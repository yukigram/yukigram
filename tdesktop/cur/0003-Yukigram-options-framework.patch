From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Yuki Sireneva <sylfn@disroot.org>
Date: Sun, 26 Apr 2026 22:07:47 +0300
Subject: [PATCH] Yukigram options framework

---
 Telegram/CMakeLists.txt                       |  4 ++
 Telegram/SourceFiles/core/launcher.cpp        |  3 +
 .../settings/settings_experimental.cpp        |  2 +
 Telegram/SourceFiles/yukigram/options.cpp     | 67 +++++++++++++++++++
 Telegram/SourceFiles/yukigram/options.h       | 21 ++++++
 .../SourceFiles/yukigram/options_database.h   | 26 +++++++
 .../SourceFiles/yukigram/options_runtime.h    | 10 +++
 7 files changed, 133 insertions(+)
 create mode 100644 Telegram/SourceFiles/yukigram/options.cpp
 create mode 100644 Telegram/SourceFiles/yukigram/options.h
 create mode 100644 Telegram/SourceFiles/yukigram/options_database.h
 create mode 100644 Telegram/SourceFiles/yukigram/options_runtime.h

diff --git a/Telegram/CMakeLists.txt b/Telegram/CMakeLists.txt
index 1f89a2926f..7e8c9e176a 100644
--- a/Telegram/CMakeLists.txt
+++ b/Telegram/CMakeLists.txt
@@ -1889,6 +1889,10 @@ PRIVATE
     window/themes/window_themes_generate_name.cpp
     window/themes/window_themes_generate_name.h
     yukigram/appid.h
+    yukigram/options.cpp
+    yukigram/options_database.h
+    yukigram/options_runtime.h
+    yukigram/options.h
     apiwrap.cpp
     apiwrap.h
     config.h
diff --git a/Telegram/SourceFiles/core/launcher.cpp b/Telegram/SourceFiles/core/launcher.cpp
index 9491e5760f..16504df5e7 100644
--- a/Telegram/SourceFiles/core/launcher.cpp
+++ b/Telegram/SourceFiles/core/launcher.cpp
@@ -19,6 +19,7 @@ https://github.com/telegramdesktop/tdesktop/blob/master/LEGAL
 #include "base/concurrent_timer.h"
 #include "base/options.h"
 #include "yukigram/appid.h"
+#include "yukigram/options_runtime.h"
 
 #include <QtCore/QLoggingCategory>
 #include <QtCore/QStandardPaths>
@@ -388,6 +389,7 @@ int Launcher::exec() {
 	// Must be started before Platform is started.
 	Logs::start();
 	base::options::init(cWorkingDir() + "tdata/experimental_options.json");
+	Yukigram::Options::init();
 
 	// Must be called after options are inited.
 	initHighDpi();
@@ -629,6 +631,7 @@ void Launcher::processArguments() {
 int Launcher::executeApplication() {
 	FilteredCommandLineArguments arguments(_argc, _argv);
 	Sandbox sandbox(arguments.count(), arguments.values());
+	Yukigram::Options::link(); // call after QApplication init
 	Ui::MainQueueProcessor processor;
 	base::ConcurrentTimerEnvironment environment;
 	return sandbox.start();
diff --git a/Telegram/SourceFiles/settings/settings_experimental.cpp b/Telegram/SourceFiles/settings/settings_experimental.cpp
index 31315bdae3..ca702f15b5 100644
--- a/Telegram/SourceFiles/settings/settings_experimental.cpp
+++ b/Telegram/SourceFiles/settings/settings_experimental.cpp
@@ -6,6 +6,7 @@ For license and copyright information please follow this link:
 https://github.com/telegramdesktop/tdesktop/blob/master/LEGAL
 */
 #include "settings/settings_experimental.h"
+#include "yukigram/options_runtime.h"
 
 #include "data/components/passkeys.h"
 #include "main/main_session.h"
@@ -144,6 +145,7 @@ void AddOption(
 			return;
 		}
 		option.set(toggled);
+		Yukigram::Options::update_value(option.id().toStdString().c_str(), toggled);
 		if (restarter) {
 			restarter->callOnce(st::settingsButtonNoIcon.toggle.duration);
 		}
diff --git a/Telegram/SourceFiles/yukigram/options.cpp b/Telegram/SourceFiles/yukigram/options.cpp
new file mode 100644
index 0000000000..7abe3b3a27
--- /dev/null
+++ b/Telegram/SourceFiles/yukigram/options.cpp
@@ -0,0 +1,67 @@
+#include "yukigram/options.h"
+#include "yukigram/options_database.h"
+#include "yukigram/options_runtime.h"
+
+namespace Yukigram::Options {
+
+std::vector<std::pair<std::string_view, std::string_view>> categories {
+	{"", ""},
+	{"interface", "Interface"},
+	{"dialogs", "Chat list"},
+	{"history", "Message history"},
+	{"compose", "Composing messages"},
+	{"system", "System"},
+};
+
+std::map<std::string_view, Toggle> toggles;
+std::map<std::string_view, std::vector<std::string_view>> toggles_by_category;
+
+rpl::variable<bool> *make(const char *key, Description &&d, bool defaultValue) {
+	const auto v = std::make_shared<rpl::variable<bool>>(defaultValue);
+
+	const auto base = d.mirrorExperimental ? std::nullopt : std::optional(
+		std::make_shared<
+			base::options::toggle,
+			base::options::descriptor<bool>
+		>({
+			.id = key,
+			.defaultValue = defaultValue,
+			.restartRequired = d.restartRequired,
+		})
+	);
+	rpl::lifetime lifetime;
+
+	const auto key_v = std::string_view(key);
+	toggles_by_category[d.category].push_back(key_v);
+	toggles[key_v] = {
+		.d = std::move(d),
+		.v = v,
+		._base = base,
+		._lifetime = std::move(lifetime),
+	};
+
+	return v.get();
+}
+
+void update_value(const char *key, bool value) {
+	if (auto it = toggles.find(std::string_view(key)); it != toggles.end()) {
+		*it->second.v = value;
+	}
+}
+
+void init() {
+	for (auto &[key, toggle] : toggles) {
+		*toggle.v = base::options::lookup<bool>(std::string(key).c_str()).value();
+	}
+}
+
+void link() {
+	for (auto &[key, toggle] : toggles) {
+		toggle.v->value() | rpl::on_next([=](bool value) {
+			base::options::lookup<bool>(std::string(key).c_str()).set(value);
+		}, toggle._lifetime);
+	}
+}
+
+
+}
diff --git a/Telegram/SourceFiles/yukigram/options.h b/Telegram/SourceFiles/yukigram/options.h
new file mode 100644
index 0000000000..77fa9cec5e
--- /dev/null
+++ b/Telegram/SourceFiles/yukigram/options.h
@@ -0,0 +1,21 @@
+#pragma once
+
+#include "rpl/rpl.h"
+
+#include <QStringList>
+#include <string_view>
+
+namespace Yukigram::Options {
+
+struct Description {
+	std::string_view name;
+	std::string_view description;
+	QStringList keywords = {};
+	std::string_view category;
+	bool restartRequired = false;
+	bool mirrorExperimental = false;
+};
+
+rpl::variable<bool> *make(const char *key, Description &&d, bool defaultValue = false);
+
+}
diff --git a/Telegram/SourceFiles/yukigram/options_database.h b/Telegram/SourceFiles/yukigram/options_database.h
new file mode 100644
index 0000000000..8ee6a5f948
--- /dev/null
+++ b/Telegram/SourceFiles/yukigram/options_database.h
@@ -0,0 +1,26 @@
+#pragma once
+
+#include "yukigram/options.h"
+#include "base/options.h"
+
+#include <map>
+#include <memory>
+#include <optional>
+#include <string_view>
+#include <vector>
+
+namespace Yukigram::Options {
+
+struct Toggle {
+	Description d;
+	std::shared_ptr<rpl::variable<bool>> v;
+	std::optional<std::shared_ptr<base::options::toggle>> _base;
+	rpl::lifetime _lifetime;
+};
+
+extern std::vector<std::pair<std::string_view, std::string_view>> categories;
+
+extern std::map<std::string_view, Toggle> toggles;
+extern std::map<std::string_view, std::vector<std::string_view>> toggles_by_category;
+
+}
diff --git a/Telegram/SourceFiles/yukigram/options_runtime.h b/Telegram/SourceFiles/yukigram/options_runtime.h
new file mode 100644
index 0000000000..1330c97017
--- /dev/null
+++ b/Telegram/SourceFiles/yukigram/options_runtime.h
@@ -0,0 +1,10 @@
+#pragma once
+
+namespace Yukigram::Options {
+
+void update_value(const char *key, bool value);
+
+void init();
+void link();
+
+}
-- 
2.53.0

