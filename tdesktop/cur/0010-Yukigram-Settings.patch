From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Yuki Sireneva <sylfn@disroot.org>
Date: Thu, 28 May 2026 20:13:50 +0000
Subject: [PATCH] (Yukigram) Settings

Co-Authored-By: Lunya <241520024+lunya-miaou@users.noreply.github.com>
---
 Telegram/CMakeLists.txt                       |   2 +
 Telegram/Resources/langs/lang.strings         |   1 +
 Telegram/Resources/langs/yukigram/en.json     |  10 ++
 .../settings/sections/settings_main.cpp       |   8 +
 .../settings/sections/settings_yukigram.cpp   | 164 ++++++++++++++++++
 .../settings/sections/settings_yukigram.h     |   9 +
 6 files changed, 194 insertions(+)
 create mode 100644 Telegram/SourceFiles/settings/sections/settings_yukigram.cpp
 create mode 100644 Telegram/SourceFiles/settings/sections/settings_yukigram.h

diff --git a/Telegram/CMakeLists.txt b/Telegram/CMakeLists.txt
index 035279a328..8d100b6fcc 100644
--- a/Telegram/CMakeLists.txt
+++ b/Telegram/CMakeLists.txt
@@ -1741,6 +1741,8 @@ PRIVATE
     settings/sections/settings_shortcuts.h
     settings/sections/settings_websites.cpp
     settings/sections/settings_websites.h
+    settings/sections/settings_yukigram.cpp
+    settings/sections/settings_yukigram.h
     storage/details/storage_file_utilities.cpp
     storage/details/storage_file_utilities.h
     storage/details/storage_settings_scheme.cpp
diff --git a/Telegram/Resources/langs/lang.strings b/Telegram/Resources/langs/lang.strings
index 3cf430918e..05011b4a9e 100644
--- a/Telegram/Resources/langs/lang.strings
+++ b/Telegram/Resources/langs/lang.strings
@@ -849,6 +849,7 @@ https://github.com/telegramdesktop/tdesktop/blob/master/LEGAL
 "lng_settings_add_account_about" = "You can add up to four accounts with different phone numbers.";
 "lng_settings_peer_to_peer_about" = "Disabling peer-to-peer will relay all calls through Telegram servers to avoid revealing your IP address, but may decrease audio and video quality.";
 "lng_settings_advanced" = "Advanced";
+"lng_settings_yukigram" = "Yukigram settings";
 "lng_settings_stickers_emoji" = "Stickers and emoji";
 "lng_settings_messages" = "Messages";
 "lng_settings_themes" = "Themes";
diff --git a/Telegram/Resources/langs/yukigram/en.json b/Telegram/Resources/langs/yukigram/en.json
index 928fa5f080..8f0b6abba4 100644
--- a/Telegram/Resources/langs/yukigram/en.json
+++ b/Telegram/Resources/langs/yukigram/en.json
@@ -4,6 +4,16 @@
     "about/3/text": "Visit {channel_link} or {faq_link} for more info.",
     "about/3/channel": "Yukigram channel",
     "intro/about": "Welcome to Yukigram, an unofficial patch-based Telegram Desktop fork.",
+    "optgroup/interface": "Interface",
+    "optgroup/tweaks": "Tweaks",
+    "optgroup/dialogs": "Chat list",
+    "optgroup/style": "Chat style",
+    "optgroup/history": "Messages",
+    "optgroup/reactions": "Reactions",
+    "optgroup/compose": "Composing messages",
+    "optgroup/picker": "Stickers and Emoji picker",
+    "optgroup/system": "System",
+    "settings/yukigram/title": "Yukigram settings",
     "unsupported-message/text": "This message is not supported by your version of Yukigram. Please update to the latest version. If you are already using the latest version, this message might depend on a feature that is not yet implemented in Telegram Desktop, or Yukigram is lagging behind upstream.",
     "unsupported-message/button/text": "View Yukigram channel",
     "_to_keep_the_diffs_nice": "Please make sure this string is kept at the bottom"
diff --git a/Telegram/SourceFiles/settings/sections/settings_main.cpp b/Telegram/SourceFiles/settings/sections/settings_main.cpp
index 3d4eda91da..59ce88648c 100644
--- a/Telegram/SourceFiles/settings/sections/settings_main.cpp
+++ b/Telegram/SourceFiles/settings/sections/settings_main.cpp
@@ -58,6 +58,7 @@ https://github.com/telegramdesktop/tdesktop/blob/master/LEGAL
 #include "settings/sections/settings_premium.h"
 #include "settings/sections/settings_privacy_security.h"
 #include "settings/settings_scale_preview.h"
+#include "settings/sections/settings_yukigram.h"
 #include "storage/localstorage.h"
 #include "ui/basic_click_handlers.h"
 #include "ui/boxes/confirm_box.h"
@@ -453,6 +454,13 @@ void BuildSectionButtons(SectionBuilder &builder) {
 		.keywords = { u"battery"_q, u"animations"_q, u"power"_q, u"saving"_q },
 	});
 
+	builder.addSectionButton({
+		.title = tr::lng_settings_yukigram(),
+		.targetSection = YukigramId(),
+		.icon = { &st::menuIconSettings },
+		.keywords = { u"yukigram"_q },
+	});
+
 	builder.addButton({
 		.id = u"main/language"_q,
 		.title = tr::lng_settings_language(),
diff --git a/Telegram/SourceFiles/settings/sections/settings_yukigram.cpp b/Telegram/SourceFiles/settings/sections/settings_yukigram.cpp
new file mode 100644
index 0000000000..909bd31369
--- /dev/null
+++ b/Telegram/SourceFiles/settings/sections/settings_yukigram.cpp
@@ -0,0 +1,164 @@
+#include "settings/sections/settings_yukigram.h"
+#include "yukigram/options_database.h"
+
+#include "settings/settings_common_session.h"
+
+#include "base/options.h"
+#include "core/application.h"
+#include "lang/lang_keys.h"
+#include "yukigram/lang.h"
+#include "settings/settings_builder.h"
+#include "settings/sections/settings_main.h"
+#include "ui/boxes/confirm_box.h"
+#include "ui/ui_utility.h"
+#include "ui/widgets/buttons.h"
+#include "ui/wrap/vertical_layout.h"
+#include "window/section_widget.h"
+#include "window/window_session_controller.h"
+#include "styles/style_menu_icons.h"
+#include "styles/style_settings.h"
+
+#include <QtGui/QGuiApplication>
+
+namespace Settings {
+
+namespace {
+
+using namespace Builder;
+
+QString qs_sv(std::string_view v) {
+	return QString::fromStdString(std::string(v));
+};
+
+void BuildToggle(
+	SectionBuilder &builder,
+	std::string_view key,
+	const Yukigram::Options::Description &d,
+	std::shared_ptr<rpl::variable<bool>> v
+) {
+	const auto icon = d.mirrorExperimental ? &st::menuIconExperimental : nullptr;
+
+	const auto button = builder.addButton({
+		.id = QString("yukigram/%1").arg(key),
+		.title = rktr(u"opt/%1/name"_q.arg(key)),
+		.st = icon ? nullptr : &st::settingsButtonNoIcon,
+		.icon = { icon },
+		.toggled = rpl::single(v->current()),
+		.keywords = std::move(d.keywords),
+	});
+
+	const auto controller = builder.controller();
+	if (!controller) {
+		return;
+	}
+
+	button->toggledValue(
+	) | rpl::filter([=](bool toggled) {
+		return toggled != v->current();
+	}) | rpl::on_next([=](bool toggled) {
+		*v = toggled;
+		if (d.restartRequired) {
+			controller->show(Ui::MakeConfirmBox({
+				.text = tr::lng_settings_need_restart(),
+				.confirmed = [] { Core::Restart(); },
+				.confirmText = tr::lng_settings_restart_now(),
+				.cancelText = tr::lng_settings_restart_later(),
+			}));
+		}
+	}, button->lifetime());
+
+	const auto descKey = u"opt/%1/desc"_q.arg(key);
+	if (!ktr(descKey).isEmpty()) {
+		builder.addSkip(st::settingsCheckboxesSkip);
+		builder.addDividerText(rktr(descKey));
+		builder.addSkip(st::settingsCheckboxesSkip);
+	}
+}
+
+const std::string_view Categories[] = {
+	"",
+	"interface",
+	"tweaks",
+	"dialogs",
+	"style",
+	"history",
+	"reactions",
+	"compose",
+	"picker",
+	"system",
+};
+
+void BuildYukigramSectionContent(SectionBuilder &builder) {
+	bool isFirstNamedSection = true;
+	auto &byCategory = Yukigram::Options::TogglesByCategory();
+	for (const auto &category : Categories) {
+		const auto &toggles = byCategory[category];
+		if (!category.empty() && !toggles.empty()) {
+			if (isFirstNamedSection) {
+				isFirstNamedSection = false;
+			} else {
+				builder.addSkip();
+				builder.addDivider();
+				builder.addSkip();
+			}
+			builder.addSubsectionTitle({
+				.id = u"yukigram/%1"_q.arg(category),
+				.title = rktr(u"optgroup/%1"_q.arg(category)),
+				.keywords = { u"yukigram"_q, qs_sv(category) },
+			});
+		}
+		for (const auto &key : toggles) {
+			auto &toggle = Yukigram::Options::Toggles()[key];
+			BuildToggle(builder, key, toggle.d, toggle.v);
+		}
+	}
+}
+
+class Yukigram : public Section<Yukigram> {
+public:
+	Yukigram(
+		QWidget *parent,
+		not_null<Window::SessionController*> controller);
+
+	[[nodiscard]] rpl::producer<QString> title() override;
+
+private:
+	void setupContent();
+
+};
+
+const auto kMeta = BuildHelper({
+	.id = Yukigram::Id(),
+	.parentId = MainId(),
+	.title = &tr::lng_settings_yukigram,
+	.icon = &st::menuIconSettings,
+}, [](SectionBuilder &builder) {
+	BuildYukigramSectionContent(builder);
+});
+
+const SectionBuildMethod kYukigramSection = kMeta.build;
+
+Yukigram::Yukigram(
+	QWidget *parent,
+	not_null<Window::SessionController*> controller)
+: Section(parent, controller) {
+	setupContent();
+}
+
+rpl::producer<QString> Yukigram::title() {
+	return rktr("settings/yukigram/title");
+}
+
+void Yukigram::setupContent() {
+	const auto content = Ui::CreateChild<Ui::VerticalLayout>(this);
+	build(content, kYukigramSection);
+	Ui::ResizeFitChild(this, content);
+}
+
+} // namespace
+
+Type YukigramId() {
+	return Yukigram::Id();
+}
+
+} // namespace Settings
\ No newline at end of file
diff --git a/Telegram/SourceFiles/settings/sections/settings_yukigram.h b/Telegram/SourceFiles/settings/sections/settings_yukigram.h
new file mode 100644
index 0000000000..2ae5460740
--- /dev/null
+++ b/Telegram/SourceFiles/settings/sections/settings_yukigram.h
@@ -0,0 +1,9 @@
+#pragma once
+
+#include "settings/settings_type.h"
+
+namespace Settings {
+
+[[nodiscard]] Type YukigramId();
+
+} // namespace Settings
-- 
2.53.0

